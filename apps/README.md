# apps/

Placeholder for future deployable applications (e.g. an API service, a web
UI). Empty by design — no tech stack has been chosen yet (see "Open
Decisions" in `docs/ARCHITECTURE.md` and `docs/DECISIONS.md`). Do not add
code here until Sprint 1 defines the stack.
