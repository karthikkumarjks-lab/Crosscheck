{
  "name": "@crosscheck/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain types for CrossCheck modules.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/registry && cp src/registry/*.json dist/registry/ && mkdir -p dist/data && cp src/data/*.json dist/data/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
